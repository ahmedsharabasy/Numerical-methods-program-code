#the cross-product between two vectors, v and w, is written as v ×w. It is defined by v ×w =norm[v]norm[w]sin(θ)

import numpy as np
v=np.array([[10,9,3]])
w=np.array([[12,5,1]])

print(np.cross(v,w))